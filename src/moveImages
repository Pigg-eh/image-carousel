function transitionImages(index) {
  const firstImg = document.querySelector(`img[data = '${index}']`);
  firstImg.classList.toggle("inactive");
  firstImg.classList.toggle("active");
}

export default transitionImages;
